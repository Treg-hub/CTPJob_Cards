#!/bin/sh
# Intentionally empty of version bumps.
#
# Build numbers (+N in pubspec.yaml) are bumped only by release rituals:
#   /mobile-app-release  → factory latest.apk
#   /mobile-pilot-release → pilot.apk
# via: node scripts/bump-build-number.js
#
# Do not re-add auto-bump here — it desyncs hosted APKs from Admin channels.
exit 0
